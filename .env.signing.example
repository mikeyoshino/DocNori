# Local compose supplies this automatically. For production set a dedicated
# PostgreSQL user/database in the protected host env file. Never commit real credentials.
SigningSessions__ConnectionString=Host=signing-db;Database=docnori_signing;Username=docnori_signing;Password=REPLACE_ON_SERVER
