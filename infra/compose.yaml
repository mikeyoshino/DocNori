name: sabuysign
services:
  web:
    image: nginx:stable-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - host
      - relay
      - media-worker
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
  host:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    volumes:
      - media-data:/media
      - template-data:/templates
      - account-keys:/keys
    environment:
      MediaConversion__Enabled: "true"
      MediaConversion__StoragePath: /media
      Templates__StoragePath: /templates
      Accounts__DataProtectionPath: /keys
      Accounts__ConnectionString: Host=postgres;Database=sabuysign;Username=sabuysign;Password=${POSTGRES_PASSWORD:-local-development-only}
      Accounts__PublicOrigin: ${ACCOUNTS_PUBLIC_ORIGIN:-}
      Accounts__Google__ClientId: ${ACCOUNTS_GOOGLE_CLIENT_ID:-}
      Accounts__Google__ClientSecret: ${ACCOUNTS_GOOGLE_CLIENT_SECRET:-}
      Accounts__Smtp__Host: ${ACCOUNTS_SMTP_HOST:-}
      Accounts__Smtp__Port: ${ACCOUNTS_SMTP_PORT:-587}
      Accounts__Smtp__User: ${ACCOUNTS_SMTP_USER:-}
      Accounts__Smtp__Password: ${ACCOUNTS_SMTP_PASSWORD:-}
      Accounts__Smtp__From: ${ACCOUNTS_SMTP_FROM:-}
      PublicOrigin: ${PUBLIC_ORIGIN:-http://localhost:8080}
      SigningSessions__ConnectionString: Host=postgres;Database=sabuysign;Username=sabuysign;Password=${POSTGRES_PASSWORD:-local-development-only}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
  media-worker:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    entrypoint: ["dotnet", "/app/worker/SabuySign.Media.Worker.dll"]
    environment:
      MediaConversion__MaxConcurrency: ${MEDIA_WORKER_CONCURRENCY:-0}
      MediaConversion__StoragePath: /media
      MediaConversion__ConnectionString: Host=postgres;Database=sabuysign;Username=sabuysign;Password=${POSTGRES_PASSWORD:-local-development-only}
    volumes:
      - media-data:/media
    depends_on:
      postgres:
        condition: service_healthy
    mem_limit: ${MEDIA_WORKER_MEMORY:-2g}
    cpus: ${MEDIA_WORKER_CPUS:-1.0}
    pids_limit: 128
    restart: unless-stopped
  relay:
    build:
      context: ..
      dockerfile: infra/Relay.Dockerfile
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 256m
    restart: unless-stopped
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: sabuysign
      POSTGRES_USER: sabuysign
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-local-development-only}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: [CMD-SHELL, "pg_isready -U sabuysign -d sabuysign"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
volumes:
  template-data:
  account-keys:
  media-data:
  postgres-data:
